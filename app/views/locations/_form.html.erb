<%= form_for @location do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title, :required => true %>

    <%= f.label :person_id %>
    <%= f.select :person_id, Person.pluck(:first_name, :id), :include_blank => true %>

    <%= f.submit %>
<% end %>